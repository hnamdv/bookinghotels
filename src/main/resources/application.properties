# ===================================================================
# APPLICATION CONFIGURATION
# ===================================================================
spring.application.name=bookinghotels

# ===================================================================
# DATASOURCE CONFIGURATION (SUPABASE POSTGRESQL)
# ===================================================================
spring.datasource.url=jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0
spring.datasource.username=postgres.kotdsvwryqwuoxjqzerx
spring.datasource.password=CONCACBAMAYDO
spring.datasource.driver-class-name=org.postgresql.Driver
# ===================================================================
# JPA / HIBERNATE CONFIGURATION
# ===================================================================
# ??nh ngh?a ph??ng ng? PostgreSQL cho Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# ===================================================================
# HIKARI CONNECTION POOL CONFIGURATION (T?i ?u k?t n?i cloud)
# ===================================================================
# Th?i gian t?i ?a ch? k?t n?i t? pool (30 giây)
spring.datasource.hikari.connection-timeout=30000
# Th?i gian t?i ?a m?t k?t n?i ???c phép r?nh trong pool tr??c khi b? gi?i phóng (10 phút)
spring.datasource.hikari.idle-timeout=600000
# Th?i gian s?ng t?i ?a c?a m?t k?t n?i trong pool (30 phút)
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.maximum-pool-size=10
# T?t tính n?ng t? ??ng c?u hình b?o m?t m?c ??nh
# Ép không dùng auto configuration c?a security
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=dia_chi_email_cua_ban@gmail.com
spring.mail.password=mat_khau_ung_dung_cua_ban
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true